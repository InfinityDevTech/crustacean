use crate::{memory::ScreepsMemory, room::cache::tick_cache::RoomCache};

pub fn attain_reservations(memory: &mut ScreepsMemory, cache: &mut RoomCache) {
    let reservation_goals = memory.goals.remote_reservation.iter_mut();


}